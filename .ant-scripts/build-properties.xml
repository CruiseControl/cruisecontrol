<?xml version="1.0"?>
<project>
    <description>
        Common properties shared across all submodules.
    </description>

    <property name="libs" location="${project.root}/libs"/>
    <property name="ant.scripts" location="${project.root}/.ant-scripts"/>

    <property file="${ant.scripts}/build.properties"/>

    <property name="remoteDebugJVMArgs" value=""/>
    <!-- To enable remote debugging of build, replace the value above with the one below. -->
    <!--property name="remoteDebugJVMArgs" value="-Xrunjdwp:transport=dt_socket,server=y,suspend=n,address=5005"/-->
</project>
